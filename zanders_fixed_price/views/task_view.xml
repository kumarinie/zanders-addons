<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Task -->
    <record id="view_task_form_inherit" model="ir.ui.view">
        <field name="name">project.task.form</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="attributes">
                <attribute name="domain">[('is_company','=',True)]</attribute>
            </field>
            <field name="user_id" position="after">
                <field name="invoice_principle"/>
                <field name="fixed_price_amount" string="Total Amount"/>
            </field>
            <xpath expr="//page[@name='users']/group/field[@name='task_user_ids']/tree" position="attributes" >
                <attribute name="editable">top</attribute>
            </xpath>
            <xpath expr="//page[@name='users']/group/field[@name='task_user_ids']/tree/field[@name='fee_rate']"
                   position="after" >
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="invoice_principle" invisible="1"/>
                <field name="allocated_percentage"
                       attrs="{'invisible': [('invoice_principle','=','tm')]}" string="Fixed fee amount"/>
                <field name="budget_hours"
                       attrs="{'invisible': [('invoice_principle','=','ff')]}"/>
            </xpath>
        </field>
    </record>

    <record id="view_task_user_tree" model="ir.ui.view">
        <field name="name">task.user.tree</field>
        <field name="model">task.user</field>
        <field name="inherit_id" ref="magnus_invoicing.view_task_user_tree"/>
        <field name="arch" type="xml">
                <field name="fee_rate" position="after">
                    <field name="start_date"/>
                    <field name="end_date"/>
                    <field name="allocated_percentage" string="Fixed fee amount"/>
                </field>
            </field>
        </record>

    <record id="view_task_user_form" model="ir.ui.view">
        <field name="name">task.user.form</field>
        <field name="model">task.user</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group col="4" cols="4">
                        <field name="user_id"/>
                        <field name="product_id"/>
                        <field name="task_id"/>
                        <field name="fee_rate" />
                        <field name="start_date"/>
                        <field name="end_date"/>
                        <field name="from_date" />
                        <field name="allocated_percentage" string="Fixed fee amount"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>